# mata

Standalone classroom app for akomanga lessons.

## Setup

Use the same Supabase project as `akomanga`.

```bash
cp .env.example .env
npm install
npm run dev
```

Open a classroom URL like:

```txt
/classroom?classId=2&lessonNumber=1
```
